import React from "react";
import { IconButtonStyle } from "../styles/IconButton";

const IconButton = ({ children }) => {
  return <IconButtonStyle> {children} </IconButtonStyle>;
};

export default IconButton;
